[project]
name = "zinecore-backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "django",
    "djangorestframework",
    "django-filter",
    "drf-spectacular",
    "psycopg[binary]",
    "gunicorn",
]

[tool.uv]
dev-dependencies = []
